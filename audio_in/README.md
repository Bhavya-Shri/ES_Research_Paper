Drop legally usable `.wav` files here (music, speech, drums).

They are picked up automatically by:

    python -m exposure_dsp experiment

If this folder is empty, the experiment still runs on built-in synthetic clips
(bass-heavy, speech-like, transient, mixed). Real program material should
replace those clips before you treat the numbers as paper results.
